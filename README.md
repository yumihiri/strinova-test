# strinova-test

玉対戦ゲーム(要件定義書セクション10「1. 土台」「2. 基本UI」の実装)。

正方形フィールド内で2つの玉がランダムに移動し、接触すると衝突ダメージ・ノックバックが発生する。
HPバー・勝敗判定・リセットボタンを備えたバトル画面のみで完結する最小構成。
CPU対CPUの観戦専用で、プレイヤーが操作できるのはリセットボタンのみ。

## 実行方法

```bash
pip install -r requirements.txt
python main.py
```
